@{
    ViewData["Title"] = "BADEA Portal";
}

@section Scripts {
    <script>
        // Card hover tilt
        document.addEventListener('DOMContentLoaded', () => {
            document.querySelectorAll('.portal-tool-card').forEach(card => {
                card.addEventListener('mousemove', (e) => {
                    const rect = card.getBoundingClientRect();
                    const x = e.clientX - rect.left - rect.width / 2;
                    const y = e.clientY - rect.top - rect.height / 2;
                    card.style.transform =
                        `translateY(-4px) rotateX(${(-y / 90)}deg) rotateY(${x / 90}deg)`;
                });
                card.addEventListener('mouseleave', () => {
                    card.style.transform =
                        'translateY(0) rotateX(0) rotateY(0)';
                });
            });

            // --- HERO BACKGROUND CAROUSEL ---
            const heroImgs = document.querySelectorAll('.portal-hero-bg-carousel img');
            if (heroImgs.length > 0) {
                let idx = 0;

                function showHero(i) {
                    heroImgs.forEach((img, k) => {
                        img.classList.toggle('active', k === i);
                    });
                }

                showHero(idx);

                // faster rotation – 2.5 seconds
                setInterval(() => {
                    idx = (idx + 1) % heroImgs.length;
                    showHero(idx);
                }, 2500);
            }
        });
    </script>
}

<style>
    /* ---------- SHARED TOKEN FOR GLASS ---------- */
    :root {
        --portal-hero-glass-bg: rgba(0, 0, 0, 0.55);
        /* slightly darker so text always readable */
    }

    html {
        scroll-behavior: smooth;
    }

    body,
    button,
    a,
    p,
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        font-family: 'Tajawal', sans-serif !important;
    }

    /* ---------- HERO ---------- */

    .portal-hero {
        border-radius: 2rem;
        overflow: hidden;
        position: relative;
        box-shadow: 0 26px 80px rgba(0, 0, 0, 0.25);
        background-color: #000;
    }

    /* stack of images under the text */
    .portal-hero-bg-carousel {
        position: absolute;
        inset: 0;
        overflow: hidden;
    }

    .portal-hero-bg-carousel img {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0;
        transition: opacity 0.9s ease-in-out;
    }

    .portal-hero-bg-carousel img.active {
        opacity: 1;
    }

    /* darker vignette so images never kill readability */
    .portal-hero-overlay {
        position: absolute;
        inset: 0;
        background:
            linear-gradient(90deg,
                rgba(0, 0, 0, 0.85) 0%,
                rgba(0, 0, 0, 0.65) 30%,
                rgba(0, 0, 0, 0.25) 60%,
                rgba(0, 0, 0, 0.05) 100%),
            radial-gradient(circle at top left, rgba(255, 255, 255, 0.18), transparent 60%);
        mix-blend-mode: normal;
    }

    .portal-hero-inner {
        position: relative;
        z-index: 1;
        padding: 2.75rem 3.5rem;
        color: #fff;
        display: flex;
        gap: 2.25rem;
        align-items: center;
    }

    .portal-hero-left {
        max-width: 460px;
    }

    /* main glass card behind hero text */
    .portal-hero-main-card {
        position: relative;
        border-radius: 2rem;
        padding: 1.9rem 2.3rem;
        background:
            radial-gradient(circle at top left, rgba(255, 255, 255, 0.08), transparent 55%),
            var(--portal-hero-glass-bg);
        box-shadow:
            0 20px 55px rgba(0, 0, 0, 0.45),
            0 0 18px rgba(0, 200, 120, 0.18);
        /* very subtle green glow */
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        overflow: hidden;
    }

    .portal-hero-main-card::before {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: inherit;
        border: 1px solid rgba(255, 255, 255, 0.10);
        box-shadow: inset 0 0 14px rgba(0, 255, 160, 0.15);
        pointer-events: none;
    }

    .portal-pill-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        padding: 0.2rem 0.8rem;
        border-radius: 999px;
        font-size: 0.7rem;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        background: rgba(0, 0, 0, 0.6);
        color: #c0f3d2;
        border: 1px solid rgba(192, 243, 210, 0.45);
    }

    .portal-hero-title {
        font-size: 2.5rem;
        font-weight: 800;
        line-height: 1.15;
        text-shadow: 0 6px 24px rgba(0, 0, 0, 0.8);
    }

    .portal-hero-sub {
        font-size: 0.95rem;
        opacity: 0.92;
    }

    /* --- PREMIUM TAJAWAL BUTTONS --- */

    .portal-hero-actions .btn {
        font-family: 'Tajawal', sans-serif !important;
        font-weight: 600;
        letter-spacing: .25px;
        /* Tajawal-friendly spacing */
        padding: 0.48rem 1.45rem;
        /* slightly more horizontal space */
        border-radius: 2.5rem;
        /* softer, cleaner pill */
        transition: all .22s ease;
        backdrop-filter: blur(4px);
        -webkit-backdrop-filter: blur(4px);
        display: inline-flex;
        align-items: center;
        gap: .35rem;
    }

    /* PRIMARY (GREEN) */
    .portal-hero-actions .btn-primary {
        background: #0a9c50;
        border-color: #0a9c50;
        box-shadow: 0 8px 22px rgba(0, 0, 0, 0.25);
    }

    .portal-hero-actions .btn-primary:hover {
        background: #07b55d;
        border-color: #07b55d;
        transform: translateY(-2px);
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.30);
    }

    /* OUTLINE LIGHT (WHITE PILL) */
    .portal-hero-actions .btn-outline-light {
        color: #ffffff !important;
        border-color: rgba(255, 255, 255, 0.55);
        background: rgba(255, 255, 255, 0.12);
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.25);
    }

    .portal-hero-actions .btn-outline-light:hover {
        background: #ffffff;
        color: #000000 !important;
        border-color: #ffffff;
        transform: translateY(-2px);
        box-shadow: 0 14px 32px rgba(0, 0, 0, 0.30);
    }

    /* OPTIONAL: micro ripple on click */
    .portal-hero-actions .btn:active {
        transform: scale(0.97);
    }

    #tools-section,
    #updates-section {
        scroll-margin-top: 96px;
        /* tweak if your topbar height changes */
    }

    /* (pulse card kept commented-out in markup) */

    /* ---------- CARDS / BLOG ---------- */

.portal-section-title {
    margin-top: 2.2rem;
    font-size: 0.9 rem;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-weight: 600;
    color: #9ca3af;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    text-rendering: optimizeLegibility;
}

/* subtle leading line, very “settings section” / Apple vibes */
.portal-section-title::before {
    content: "";
    display: inline-block;
    width: 28px;
    height: 1px;
    border-radius: 999px;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.08), rgba(0, 0, 0, 0.28));
}


    .portal-tool-card {
        border-radius: 1.5rem;
        border: 1px solid #e4e6eb;
        padding: 1.3rem 1.5rem;
        background: linear-gradient(145deg, #ffffff, #f7fafc);
        box-shadow:
            0 10px 28px rgba(15, 29, 45, 0.06),
            0 0 0 rgba(0, 150, 57, 0.0);
        transition:
            transform .25s ease,
            box-shadow .25s ease,
            border-color .25s ease,
            background .25s ease;
        cursor: pointer;
    }

    .portal-tool-card:hover {
        box-shadow:
            0 18px 40px rgba(15, 29, 45, 0.14),
            0 0 24px rgba(0, 150, 57, 0.12);
        border-color: rgba(0, 150, 57, 0.22);
        background: linear-gradient(145deg, #ffffff, #f3f8ff);
        transform: translateY(-4px);
    }
    /* ---------- CARD HEIGHT NORMALIZATION ---------- */

    /* Make each column act as a flex container so the card can stretch full height */
#updates-section .row.g-3 > [class*="col-"] {
    display: flex;
}

#updates-section .portal-blog-card {
    width: 100%;
    display: flex;
    flex-direction: column;
}

#updates-section .portal-blog-card p.small {
    display: -webkit-box;
    -webkit-line-clamp: 3;      /* number of lines before truncating */
    -webkit-box-orient: vertical;
    overflow: hidden;
}

    /* Clamp the description text to 3 lines with ellipsis */
    .portal-tool-body p.small.text-muted {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    /* new flex layout: text on left, logo/icon on right */
    .portal-tool-layout {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1.25rem;
    }

    .portal-tool-body {
        flex: 1;
        min-width: 0;
    }

    .portal-tool-visual {
        flex-shrink: 0;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .portal-tool-title {
        margin-bottom: 0.15rem;
    }

    .portal-tool-icon {
        width: 54px;
        height: 54px;
        border-radius: 1.1rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: var(--badea-green-soft);
        color: var(--badea-green);
        font-size: 1.5rem;
        box-shadow: 0 10px 26px rgba(15, 29, 45, 0.18);
    }

    .portal-tool-logo-img,
    .portal-tool-icon-img {
        width: 126px;
        height: 126px;
        border-radius: 1.75rem;
        object-fit: contain;
        background: radial-gradient(circle at top, #ffffff, #eef3f8);
        box-shadow: 0 12px 30px rgba(15, 29, 45, 0.18);
    }

    .portal-tool-meta {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        color: #8b9097;
    }

    .portal-chip {
        display: inline-flex;
        align-items: center;
        gap: 0.25rem;
        padding: 0.25rem 0.7rem;
        border-radius: 999px;
        font-size: 0.7rem;
        background: #f1faf5;
        color: #137c3e;
        border: 1px solid #cfead7;
    }

    .portal-blog-card {
        border-radius: 1.25rem;
        background: #fff;
        border: 1px solid #e5e7eb;
        padding: 1.25rem 1.4rem;
        transition: box-shadow .2s ease, transform .2s ease;
    }

    .portal-blog-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 16px 36px rgba(15, 29, 45, 0.08);
    }

    /* ---------- RESPONSIVE ---------- */

    @@media (max-width: 991.98px) {
        .portal-hero-inner {
            flex-direction: column;
            padding: 2.25rem 2rem;
        }

        .portal-hero-left {
            max-width: none;
        }
    }

    @@media (max-width: 767.98px) {
        .portal-hero-inner {
            padding: 2rem 1.5rem;
        }

        .portal-hero-title {
            font-size: 2.05rem;
        }

        .portal-hero-main-card {
            padding: 1.5rem 1.6rem;
        }

        .portal-tool-layout {
            align-items: flex-start;
            gap: 0.9rem;
        }

        .portal-tool-visual {
            align-self: stretch;
        }

        .portal-tool-logo-img,
        .portal-tool-icon-img {
            width: 80px;
            height: 80px;
        }
    }
</style>


<div class="container-fluid px-0">
    <!-- HERO -->
    <section class="mb-4">
        <div class="portal-hero">
            <!-- background carousel under the text -->
            <div class="portal-hero-bg-carousel">
                <img src="~/images/portal-hero.jpg" alt="BADEA hero 1" />
                <img src="~/images/portal-hero2.jpg" alt="BADEA hero 2" />
                <img src="~/images/portal-hero3.jpg" alt="BADEA hero 3" />
                <img src="~/images/portal-hero4.jpg" alt="BADEA hero 4" />
                <img src="~/images/portal-hero5.jpg" alt="BADEA hero 5" />

            </div>

            <div class="portal-hero-overlay"></div>

            <div class="portal-hero-inner">
                <div class="portal-hero-left">
                    <div class="portal-hero-main-card">
                        <div class="portal-pill-badge mb-2">
                            <span>BADEA internal portal</span>
                        </div>
                        <h1 class="portal-hero-title mb-2">
                            One place for your BADEA tools and insights.
                        </h1>
                        <p class="portal-hero-sub mb-3">
                            Access BADEA’s core tools in one place — from finance and HR to missions, workflows, and
                            strategic KPIs.

                        </p>
                        <div class="portal-hero-actions d-flex flex-wrap gap-2 mt-2">
                            <a href="#tools-section" class="btn btn-primary btn-sm px-4">
                                Browse apps & tools
                            </a>
                            <a href="#updates-section" class="btn btn-outline-light btn-sm px-4">
                                View BADEA updates
                            </a>
                        </div>

                    </div>
                </div>

                @* Pulse card kept commented out for now *@
                @* <div class="portal-hero-pulse">
                    ...
                </div> *@
            </div>
        </div>
    </section>
    <!-- BLOG / UPDATES -->
    <section id="updates-section" class="mt-4">
        <div class="d-flex justify-content-between align-items-center mb-2">
            <div>
                <div class="portal-section-title">BADEA updates</div>
                @* <div class="text-muted small">Lightweight internal news stream (static for now).</div> *@
            </div>
        </div>

        <div class="row g-3">
            <div class="col-lg-4 col-md-6">
                <article class="portal-blog-card">
                    <div class="small text-muted mb-1">March 2025</div>
                    <h6 class="mb-1">Portal design draft ready</h6>
                    <p class="small mb-0 text-muted">
                        First version of the BADEA internal portal layout – we’ll plug it into existing tools next.
                    </p>
                </article>
            </div>
            <div class="col-lg-4 col-md-6">
                <article class="portal-blog-card">
                    <div class="small text-muted mb-1">Coming soon</div>
                    <h6 class="mb-1">Single entry for KPIs &amp; actions</h6>
                    <p class="small mb-0 text-muted">
                        The portal will become the front entry for dashboards, RedBoard, and action plans.
                    </p>
                </article>
            </div>
            <div class="col-lg-4 col-md-6">
                <article class="portal-blog-card">
                    <div class="small text-muted mb-1">Coming soon</div>
                    <h6 class="mb-1">BADEA guidelines &amp; resources</h6>
                    <p class="small mb-0 text-muted">
                        A space for templates, policies, and reference documents – one place instead of many links.
                    </p>
                </article>
            </div>
        </div>
    </section>
    <!-- TOOLS & WORKSPACES -->
    <section id="tools-section" class="mb-4">
        <div class="d-flex justify-content-between align-items-end mb-2">
            <div>
                <div class="portal-section-title">Tools &amp; workspaces</div>
            </div>
            @* <span class="portal-chip">
                • Internal
                <span class="badge bg-success rounded-pill ms-1">BADEA</span>
            </span> *@
        </div>
        <div class="row g-3">

            <!-- 1. Missions Management -->
            <div class="col-lg-4 col-md-6">
                <a href="https://portal.badea.org/ords/r/badea/mofad/" target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">Missions Management</h5>
                            <div class="portal-tool-meta mb-2">Travel · Approvals</div>
                            <p class="mb-3 small text-muted">
                                Submit and track mission requests, approvals, and travel workflows.
                            </p>
                            <span class="portal-chip">Internal</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/MOFAD.png" alt="Missions icon" class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 2. SAP Employee Central -->
            <div class="col-lg-4 col-md-6">
                <a href="https://hcm55.sapsf.eu/login?company=arabbankfo&_s.crb=ucnoZgVwtxw7m4mncpVzAllNfSvR9S6JSu2rTdH68Ww%3d#/login"
                    target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">SAP Employee Central</h5>
                            <div class="portal-tool-meta mb-2">HR · Profiles</div>
                            <p class="mb-3 small text-muted">
                                Access personal data, leave balances, HR services, and employee records.
                            </p>
                            <span class="portal-chip">SAP</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/SAP-Success-Factor-HCM.png" alt="SAP Employee Central icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 3. Freshworks (Support) -->
            <div class="col-lg-4 col-md-6">
                <a href="https://support.badea.org/support/home" target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">Freshworks Support</h5>
                            <div class="portal-tool-meta mb-2">Support · Tickets</div>
                            <p class="mb-3 small text-muted">
                                Submit IT and service tickets, track issues, and interact with support teams.
                            </p>
                            <span class="portal-chip">Support</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/Freshworks.png" alt="Freshworks icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 4. Laserfiche -->
            <div class="col-lg-4 col-md-6">
                <a href="https://10.3.0.82/laserfiche" target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">Laserfiche</h5>
                            <div class="portal-tool-meta mb-2">Documents · Workflow</div>
                            <p class="mb-3 small text-muted">
                                Access automated workflows, forms, and digital document archiving.
                            </p>
                            <span class="portal-chip">Docs</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/Laserfiche.png" alt="Laserfiche icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 5. Operational Risk Management -->
            <div class="col-lg-4 col-md-6">
                <a href="https://portal.badea.org/ords/r/badea/operation-risk-events-management110/login"
                    target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">Operational Risk Management</h5>
                            <div class="portal-tool-meta mb-2">Risk · Controls</div>
                            <p class="mb-3 small text-muted">
                                Report incidents, track controls, and manage operational risk events.
                            </p>
                            <span class="portal-chip">Compliance</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/ORM.png" alt="ORM icon" class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 6. SAP Fiori -->
            <div class="col-lg-4 col-md-6">
                <a href="https://vharfws4wd01.sap.badea.org:44380/sap/bc/ui2/flp?sap-client=100&amp;sap-language=EN"
                    target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">SAP Fiori</h5>
                            <div class="portal-tool-meta mb-2">SAP · Processes</div>
                            <p class="mb-3 small text-muted">
                                Open SAP Fiori apps for approvals, workflows, and daily operations.
                            </p>
                            <span class="portal-chip">SAP</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/SAP-FIORI-LOGO.png" alt="SAP Fiori icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 7. BADEA Services Portal (Oracle APEX) -->
            <div class="col-lg-4 col-md-6">
                <a href="http://10.3.0.249:8080/ords/r/badea/services-portal-%D8%A8%D9%88%D8%A7%D8%A8%D8%A9-%D8%A7%D9%84%D8%AE%D8%AF%D9%85%D8%A7%D8%AA/login"
                    target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">BADEA Services Portal</h5>
                            <div class="portal-tool-meta mb-2">Services · Requests</div>
                            <p class="mb-3 small text-muted">
                                Submit and follow up on internal BADEA service requests in one place.
                            </p>
                            <span class="portal-chip">Services</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/BADEAServicelogo.png" alt="BADEA Services Portal icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>

            <!-- 8. KPI Monitor -->
            <div class="col-lg-4 col-md-6">
                <a href="https://kpimonitor.badea.org" target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">KPI Monitor</h5>
                            <div class="portal-tool-meta mb-2">Dashboards · Insights</div>
                            <p class="mb-3 small text-muted">
                                View KPI dashboards, monitor performance, and follow up on strategic indicators.
                            </p>
                            <span class="portal-chip">Analytics</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/kpimonitor.png" alt="KPI Monitor icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>
            <!-- 9. ECM -->
            <div class="col-lg-4 col-md-6">
                <a href="https://ecm.badea.org/WebPortal" target="_blank" rel="noopener noreferrer"
                    class="portal-tool-card text-decoration-none text-dark d-block">
                    <div class="portal-tool-layout">
                        <div class="portal-tool-body">
                            <h5 class="mb-1 portal-tool-title">Enterprise Content Management</h5>
                            <div class="portal-tool-meta mb-2">Dashboards · Insights</div>
                            <p class="mb-3 small text-muted">
                                View KPI dashboards, monitor performance, and follow up on strategic indicators.
                            </p>
                            <span class="portal-chip">Analytics</span>
                        </div>
                        <div class="portal-tool-visual">
                            <img src="~/images/icons/ecm.png" alt="KPI Monitor icon"
                                class="portal-tool-logo-img" />
                        </div>
                    </div>
                </a>
            </div>
        </div>
    </section>


</div>