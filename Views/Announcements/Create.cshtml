@model BADEAPORTAL.Models.Announcements.AnnouncementCreateVm

@{
    ViewData["Title"] = "New announcement";
}

<link href="https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.snow.css" rel="stylesheet">

<style>
    /* ================= PORTAL / APPLE LOOK (UNCHANGED) ================= */

    :root {
        --portal-hero-glass-bg: rgba(0, 0, 0, .55);
    }

    body,
    button,
    a,
    p,
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        font-family: 'Tajawal', sans-serif !important;
    }

    .create-shell {
        max-width: 1040px;
        margin: 1.75rem auto 0 auto;
    }

    .portal-hero {
        border-radius: 2rem;
        overflow: hidden;
        position: relative;
        box-shadow: 0 26px 80px rgba(0, 0, 0, .25);
        background: #000;
        min-height: 320px;
    }

    .portal-hero-bg-carousel img {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .portal-hero-overlay {
        position: absolute;
        inset: 0;
        background:
            linear-gradient(90deg,
                rgba(0, 0, 0, .86) 0%,
                rgba(0, 0, 0, .62) 35%,
                rgba(0, 0, 0, .18) 70%,
                rgba(0, 0, 0, .05) 100%),
            radial-gradient(circle at top left, rgba(255, 255, 255, .18), transparent 60%);
    }

    .portal-hero-inner {
        position: relative;
        z-index: 1;
        padding: 2.4rem 3.2rem;
        color: #fff;
        display: flex;
        justify-content: space-between;
        gap: 1.5rem;
        flex-wrap: wrap;
    }

    .portal-pill-badge {
        display: inline-flex;
        padding: .2rem .8rem;
        border-radius: 999px;
        font-size: .7rem;
        text-transform: uppercase;
        letter-spacing: .08em;
        background: rgba(0, 0, 0, .6);
        color: #c0f3d2;
        border: 1px solid rgba(192, 243, 210, .45);
    }

    .hero-title {
        font-size: 2.1rem;
        font-weight: 850;
        text-shadow: 0 8px 26px rgba(0, 0, 0, .85);
    }

    .hero-sub {
        font-size: .95rem;
        opacity: .92;
    }

    .apple-card {
        border-radius: 1.8rem;
        background: #fff;
        border: 1px solid #e5e7eb;
        box-shadow: 0 18px 60px rgba(15, 29, 45, .12);
        overflow: hidden;
    }

    .apple-card-header {
        padding: 1.15rem 1.4rem;
        background: linear-gradient(180deg, #fbfdff, #fff);
        border-bottom: 1px solid #eef2f7;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .toggle-pill {
        display: inline-flex;
        gap: .55rem;
        padding: .35rem .85rem;
        border-radius: 999px;
        border: 1px solid #e5e7eb;
        background: #fff;
        box-shadow: 0 10px 26px rgba(15, 29, 45, .07);
    }

    .apple-card-body {
        padding: 1.35rem 1.4rem;
    }

    .field-label {
        font-size: .78rem;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: .1em;
        color: #6b7280;
        margin-bottom: .35rem;
    }

    .premium-input {
        border-radius: 1.05rem !important;
        border: 1px solid #e5e7eb !important;
        box-shadow: 0 12px 28px rgba(15, 29, 45, .06);
    }

    /* ================= QUILL (MATCH MEMO GENERATOR) ================= */

    .premium-editor-shell {
        border-radius: 1.4rem;
        border: 1px solid #e5e7eb;
        background: #fff;
        box-shadow: 0 18px 60px rgba(15, 29, 45, .12);
        overflow: hidden;
    }

    .premium-editor-toolbar {
        border-bottom: 1px solid #eef2f7;
        background: linear-gradient(180deg, #fbfdff, #fff);
        padding: .35rem .5rem;
    }

    .premium-editor {
        min-height: 320px;
        padding: 1rem;
    }

    .ql-editor table {
        width: 100%;
        border-collapse: collapse;
    }

    .ql-editor td,
    .ql-editor th {
        border: 1px solid #e5e7eb;
        padding: 6px;
    }

    /* ================= MEMO PANEL ================= */

    .memo-panel {
        border-radius: 1.4rem;
        border: 1px solid #eef2f7;
        background: linear-gradient(180deg, #fbfdff, #fff);
        padding: 1.1rem;
        margin-top: 1rem;
        display: none;
    }

    .memo-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
    }
</style>

<div class="container-fluid px-0">
    <section class="mb-4">
        <div class="portal-hero">
            <div class="portal-hero-bg-carousel">
                <img src="~/images/announcement-hero.jpg" />
            </div>
            <div class="portal-hero-overlay"></div>

            <div class="portal-hero-inner">
                <div>
                    <div class="portal-pill-badge">BADEA · Create</div>
                    <div class="hero-title">New announcement.</div>
                    @* <div class="hero-sub">Optional memo header · Rich content · RTL support</div> *@
                </div>
                <a asp-action="Index" class="btn btn-outline-light">Back</a>
            </div>
        </div>
    </section>

    <div class="create-shell">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <div class="apple-card">
                <div class="apple-card-header">
                    <div class="field-label">Announcement content</div>
                    <div class="toggle-pill">
                        <!-- real bound field -->
                        <input asp-for="IsMemo" type="hidden" id="isMemoHidden" />

                        <!-- UI checkbox (inverse meaning) -->
                        <input type="checkbox" id="isAnnouncementToggle" class="form-check-input m-0" />
                        <label for="isAnnouncementToggle">This is an announcement</label>
                    </div>

                </div>

                <div class="apple-card-body">

                    <div class="mb-3">
                        <div class="field-label">Title</div>
                        <input asp-for="Title" class="form-control premium-input" />
                    </div>

                    <div id="memoFields" class="memo-panel">
                        <div class="memo-grid">

                            <!-- TO (employees quick search) -->
                            <div>
                                <div class="field-label">To</div>
                                <input asp-for="MemoTo" id="memoToInput" class="form-control premium-input"
                                       placeholder="Search employee..." autocomplete="off" />
                            </div>

                            <!-- FROM KIND -->
                            <div>
                                <div class="field-label">From type</div>
                                <select asp-for="FromKind" id="fromKindSelect" class="form-select premium-input">
                                    <option value="USER">User</option>
                                    <option value="DEPT">Department</option>
                                </select>
                            </div>

                            <!-- FROM (dynamic: user search or dept search) -->
                            <div>
                                <div class="field-label">From</div>
                                <input asp-for="MemoFrom" id="memoFromInput" class="form-control premium-input"
                                       placeholder="Search..." autocomplete="off" />
                                <input asp-for="FromDeptCode" type="hidden" id="fromDeptCodeHidden" />
                            </div>

                            <!-- THROUGH (departments only) -->
                            <div>
                                <div class="field-label">Through (Department)</div>
                                <input asp-for="MemoThrough" id="memoThroughInput" class="form-control premium-input"
                                       placeholder="Search department..." autocomplete="off" />
                            </div>

                            <div>
                                <div class="field-label">Subject</div>
                                <input asp-for="MemoSubject" class="form-control premium-input" placeholder="Subject" />
                            </div>

                            <div>
                                <div class="field-label">Classification</div>
                                <input asp-for="MemoClassification" class="form-control premium-input"
                                       placeholder="Classification" />
                            </div>

                            <!-- notifications -->
                            <div style="grid-column: 1 / -1;">
                                <div class="field-label">Notifications</div>
                                <div class="d-flex gap-4 align-items-center">
                                    <label class="d-flex align-items-center gap-2">
                                        <input asp-for="NotifyInApp" class="form-check-input m-0" />
                                        <span>In-app notification</span>
                                    </label>

                                    <label class="d-flex align-items-center gap-2">
                                        <input asp-for="NotifyEmail" class="form-check-input m-0" />
                                        <span>Also send email (later)</span>
                                    </label>
                                </div>
                            </div>

                        </div>
                    </div>


                    <div class="mt-4">
                        <div class="field-label">Body</div>

                        <input asp-for="BodyHtml" type="hidden" id="BodyHtml" />

                        <div class="premium-editor-shell">
                            <div id="q-toolbar" class="premium-editor-toolbar">
                                <button class="ql-bold"></button>
                                <button class="ql-italic"></button>
                                <button class="ql-underline"></button>
                                <select class="ql-align"></select>
                                <button class="ql-list" value="bullet"></button>
                                <button class="ql-list" value="ordered"></button>
                                <button class="ql-direction" value="rtl"></button>
                                <button id="tbl-insert" type="button">▦</button>
                            </div>
                            <div id="q-editor" class="premium-editor"></div>
                        </div>
                    </div>

                    <div class="mt-4 d-flex gap-2">
                        <button class="btn btn-success">Publish</button>
                        <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                    </div>

                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.js"></script>

    <script>
        const quill = new Quill('#q-editor', {
            theme: 'snow',
            modules: { toolbar: '#q-toolbar', table: true }
        });

        const body = document.getElementById('BodyHtml');
        if (body.value) quill.clipboard.dangerouslyPasteHTML(body.value);

        document.querySelector('form').addEventListener('submit', () => {
            body.value = quill.root.innerHTML;
        });

        const isMemoHidden = document.getElementById('isMemoHidden');
        const isAnnouncementToggle = document.getElementById('isAnnouncementToggle');
        const panel = document.getElementById('memoFields');

        function syncMemoToggle() {
            const isAnnouncement = isAnnouncementToggle.checked;
            isMemoHidden.value = (!isAnnouncement).toString();   // memo if NOT announcement
            panel.style.display = (!isAnnouncement) ? 'block' : 'none';
        }

        // default memo
        isAnnouncementToggle.checked = false;
        syncMemoToggle();
        isAnnouncementToggle.addEventListener('change', syncMemoToggle);

        document.querySelector('form').addEventListener('submit', e => {
            const body = quill.getText().trim();
            if (!body.length) {
                e.preventDefault();
                alert('Body cannot be empty');
                return;
            }

            const isMemo = (isMemoHidden.value === 'true');
            if (isMemo) {
                const required = ['MemoTo', 'MemoFrom', 'MemoSubject', 'MemoClassification'];
                for (const name of required) {
                    const el = document.querySelector(`[name="${name}"]`);
                    if (!el.value.trim()) {
                        e.preventDefault();
                        el.focus();
                        alert('Please complete memo header fields');
                        return;
                    }
                }
            }
        });

        async function searchEmployees(q) {
            const res = await fetch(`/api/lookup/employees?q=${encodeURIComponent(q)}&take=10`);
            if (!res.ok) return [];
            return await res.json();
        }

        async function searchDepartments(q) {
            const res = await fetch(`/api/lookup/departments?q=${encodeURIComponent(q)}&take=10`);
            if (!res.ok) return [];
            return await res.json();
        }

        function attachTypeahead(inputEl, fetchFn, onPick) {
            let box;
            let t;

            function closeBox() {
                if (box) { box.remove(); box = null; }
            }

            function render(items) {
                closeBox();
                if (!items.length) return;

                box = document.createElement('div');
                box.style.position = 'absolute';
                box.style.zIndex = '9999';
                box.style.background = '#fff';
                box.style.border = '1px solid #e5e7eb';
                box.style.borderRadius = '12px';
                box.style.boxShadow = '0 18px 60px rgba(15,29,45,.12)';
                box.style.overflow = 'hidden';
                box.style.maxHeight = '280px';
                box.style.overflowY = 'auto';

                const rect = inputEl.getBoundingClientRect();
                box.style.left = `${rect.left + window.scrollX}px`;
                box.style.top = `${rect.bottom + window.scrollY + 6}px`;
                box.style.width = `${rect.width}px`;

                items.forEach(item => {
                    const row = document.createElement('div');
                    row.textContent = item.label || item.Label || '';
                    row.style.padding = '10px 12px';
                    row.style.cursor = 'pointer';
                    row.addEventListener('mousedown', (e) => {
                        e.preventDefault();
                        onPick(item);
                        closeBox();
                    });
                    box.appendChild(row);
                });

                document.body.appendChild(box);
            }

            const onInput = () => {
                const q = inputEl.value.trim();
                clearTimeout(t);
                if (q.length < 2) { closeBox(); return; }

                t = setTimeout(async () => {
                    const items = await fetchFn(q);
                    render(items);
                }, 180);
            };

            const onDocClick = (e) => {
                if (e.target !== inputEl) closeBox();
            };

            const onResize = () => closeBox();
            const onScroll = () => closeBox();

            inputEl.addEventListener('input', onInput);
            document.addEventListener('click', onDocClick);
            window.addEventListener('resize', onResize);
            window.addEventListener('scroll', onScroll, true);

            return () => {
                clearTimeout(t);
                inputEl.removeEventListener('input', onInput);
                document.removeEventListener('click', onDocClick);
                window.removeEventListener('resize', onResize);
                window.removeEventListener('scroll', onScroll, true);
                closeBox();
            };
        }

        // Wire fields
        const memoToInput = document.getElementById('memoToInput');
        attachTypeahead(memoToInput, searchEmployees, (item) => {
            memoToInput.value = item.label || item.Label || '';
        });

        const memoThroughInput = document.getElementById('memoThroughInput');
        attachTypeahead(memoThroughInput, searchDepartments, (item) => {
            memoThroughInput.value = item.label || item.Label || '';
        });

        const fromKindSelect = document.getElementById('fromKindSelect');
        const memoFromInput = document.getElementById('memoFromInput');
        const fromDeptCodeHidden = document.getElementById('fromDeptCodeHidden');

        let disposeFromPicker = null;

        function wireFromPicker() {
            if (disposeFromPicker) {
                disposeFromPicker();
                disposeFromPicker = null;
            }

            fromDeptCodeHidden.value = "";
            memoFromInput.value = "";

            if (fromKindSelect.value === "DEPT") {
                memoFromInput.placeholder = "Search department...";
                disposeFromPicker = attachTypeahead(memoFromInput, searchDepartments, (item) => {
                    memoFromInput.value = item.deptName || item.DeptName || (item.label || item.Label || '');
                    fromDeptCodeHidden.value = item.deptCode || item.DeptCode || '';
                });
            } else {
                memoFromInput.placeholder = "Search employee...";
                disposeFromPicker = attachTypeahead(memoFromInput, searchEmployees, (item) => {
                    memoFromInput.value = item.label || item.Label || '';
                });
            }
        }

        fromKindSelect.addEventListener('change', wireFromPicker);
        wireFromPicker();
    </script>
}
